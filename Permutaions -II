# Python-3
from typing import List
import itertools
class Solution:
    def permuteUnique(self, nums: List[int]) -> List[List[int]]:
        result = set(itertools.permutations(nums))
        return[list(p) for p in result]
        
